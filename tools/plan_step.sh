#!/usr/bin/env bash

# @tool Declare the next step you will take toward the overall goal. Use this before taking action.
# @param step:string(required) Description of the next step
# @param rationale:string(optional) Why this step is needed

# plan_step is a virtual tool — it is handled directly in loop.sh,
# not executed as a regular tool script.

echo "Step recorded. Now execute this step by calling the appropriate tool (e.g., run_shell, read_file). Do NOT just plan — take action."
